"""
Конфигурация и глобальные константы GearSolver.
"""

# Допуски и пороги
TOLERANCE = 5.0  # Допустимое суммарное отклонение (мм)

# Погрешность измерений (мм) — используется как σ для confidence
MEASUREMENT_TOLERANCE = 0.3

# Весовые коэффициенты невязок по типам измерений
# Базовая эвристика: df и aw обычно надёжнее, da менее надёжен при износе вершин
WEIGHT_DA = 0.8
WEIGHT_DF = 1.2
WEIGHT_AW = 1.5

# Весовые коэффициенты для толщин зуба
WEIGHT_STIP = 1.4
WEIGHT_SROOT = 1.1

# Стандартные дискретные ряды для перебора в solver
ROOT_FILLET_COEFFS = [0.2, 0.25, 0.3, 0.35, 0.375, 0.4, 0.45, 0.5]
C_STAR_VALUES = [0.157, 0.2, 0.25, 0.3, 0.35, 0.4]

# Штраф за отклонение ha* от 1.0 (вес регуляризации для stage=2)
REGULARIZATION_WEIGHT = 4.0

# Порог ошибки для перехода ко второму этапу (мм, RMSE)
STAGE1_THRESHOLD = 0.5  # Если RMSE этапа 1 > порога, запускаем этап 2

# Параметры пула модулей
MODULE_STEP = 0.01
MODULE_MIN = 0.5
MODULE_MAX = 10.0

# Стандартные метрические модули (ГОСТ)
GOST_MODULES = [
    0.5, 0.8, 1.0, 1.25, 1.5, 2.0, 2.25, 2.5, 3.0, 3.15, 3.5, 4.0, 4.5, 5.0,
    6.0, 8.0, 10.0, 12.0, 16.0, 20.0, 25.0, 32.0, 40.0, 50.0
]

# Дюймовые питчи (DP) для конвертации в метрический модуль
DP_PITCHES = [4, 5, 6, 8, 10, 12, 16, 20, 24, 32, 48]

# Границы (bounds) для параметров оптимизации
X_BOUNDS = (-1.5, 1.5)              # Коэффициент смещения x
HA_BOUNDS = (0.5, 1.2)              # Коэффициент высоты головки ha*
C_BOUNDS = (0.15, 0.4)              # Коэффициент радиального зазора c*

# Вывод результатов
TOP_N = 20  # Количество топ-результатов для вывода в таблицу

# Допуск при проверке совпадения с ГОСТ/DP (мм)
STANDARD_MATCH_TOLERANCE = 0.005

# Допуски по толщине зуба (мм)
TIP_THICKNESS_TOLERANCE = 0.20    # Малый допуск для толщины вершины
ROOT_THICKNESS_TOLERANCE = 0.35   # Толщина у основания менее точна из-за скругления

# Минимально допустимые технологические толщины (мм)
MIN_TIP_THICKNESS = 0.8
MIN_ROOT_THICKNESS = 1.0
