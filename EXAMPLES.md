# GearSolver — Примеры использования

## 📚 Быстрый старт

### Сценарий 1: Анализ одной шестерни (CLI)

Вам известны диаметры вершин и впадин, а также число зубьев:

```bash
python gear_calc.py 52 46 20
```

**Что происходит:**
1. Скрипт загружает параметры: da₁=52мм, df₁=46мм, z₁=20
2. Генерирует пул из ~110 модулей (сетка + ГОСТ + DP)
3. Для каждого модуля оптимизирует x₁, ha*, c* параллельно
4. Выводит топ-20 результатов с минимальной ошибкой
5. Генерирует интерактивный дашборд в `gear_results.html`

**Типичный вывод:**
```
✓ Входные данные загружены
  Тип: одна шестерня
  Шестерня: da1=52.0, df1=46.0, z1=20

✓ Формирование пула модулей...
  Всего модулей для проверки: 110

✓ Оптимизация (мультипроцессинг)...
  Завершено 110 итераций

✓ Фильтрация по допуску 5.0 мм...

GearSolver — Топ результатов
┏━━━┳━━━━━━━━━━┳━━━━━━━━━━━━┳━━━━━━━━┳━━━━━━━━┳━━━━━━━━━┳━━━━━━━━┳━━━━━━━━━━━━━┓
┃ # ┃   m (мм) ┃  Стандарт  ┃     x₁ ┃     x₂ ┃     hₐ* ┃     c* ┃ Ошибка (мм) ┃
┡━━━╇━━━━━━━━━━╇━━━━━━━━━━━━╇━━━━━━━━╇━━━━━━━━╇━━━━━━━━━╇━━━━━━━━╇━━━━━━━━━━━━━┩
│ 1 │    2.400 │     —      │  0.285 │      — │   0.548 │  0.154 │      0.0000 │
│ 2 │    2.250 │   ГОСТ *   │  0.982 │      — │   0.573 │  0.187 │      0.0000 │
│ 3 │    2.300 │     —      │  0.730 │      — │   0.575 │  0.155 │      0.0000 │
│ 4 │    2.540 │   DP 10    │ -0.274 │      — │   0.511 │  0.160 │      0.0000 │
...
```

**Интерпретация результатов:**

| Поле | Значение | Смысл |
|------|----------|-------|
| `m = 2.400` | модуль | Найденный шаг зацепления |
| `x₁ = 0.285` | смещение | Коэффициент смещения инструмента (+0.285 → увеличение диаметра) |
| `ha* = 0.548` | высота головки | Относительная высота головки зуба (от стандартной 1.0) |
| `c* = 0.154` | радиальный зазор | Относительный зазор между зубьями |
| `Ошибка = 0.0000` | невязка | Расхождение с исходными диаметрами (0 = идеально совпадает) |
| `ГОСТ *` | стандарт | Модуль совпадает со стандартом ГОСТ 9563 |

**Выбор лучшего результата:**
- Ищите минимальную ошибку
- Приоритет ГОСТ модули (маркировка `*`) — они доступнее на производстве
- Если нужен стандартный инструмент — обратите внимание на `DP` маркировку

---

### Сценарий 2: Анализ пары с межосевым расстоянием (CLI)

Известны оба элемента передачи и желаемое расстояние между осями:

```bash
python gear_calc.py 50 45 20 100 95 40 75
```

**Параметры:**
- Шестерня 1: da₁=50мм, df₁=45мм, z₁=20
- Шестерня 2: da₂=100мм, df₂=95мм, z₂=40
- Межосевое расстояние: aw=75мм

**Особенность для пары:**
- Система ищет общий модуль `m` для обеих шестерён
- Коэффициент смещения `x₁` и `x₂` оптимизируются независимо
- Коэффициенты `ha*` и `c*` — одинаковые для обеих (физически корректно)
- Дополнительное ограничение: `aw = m*(z1+z2)/2 + m*(x1+x2)`

**Вывод содержит x₂:**
```
┃ # ┃   m (мм) ┃  Стандарт  ┃     x₁ ┃     x₂ ┃     hₐ* ┃     c* ┃ Ошибка (мм) ┃
┡━━━╇━━━━━━━━━━╇━━━━━━━━━━━━╇━━━━━━━━╇━━━━━━━━╇━━━━━━━━━╇━━━━━━━━╇━━━━━━━━━━━━━┩
│ 1 │    2.300 │     —      │  0.401 │  1.271 │   0.500 │  0.150 │      0.2900 │
```

Здесь видно, что для достижения нужного межосевого расстояния, пиньон смещается `x₁=+0.401`, а колесо `x₂=+1.271`.

---

### Сценарий 3: Интерактивный режим

Если не помните точный синтаксис — просто запустите без аргументов:

```bash
python gear_calc.py
```

**Диалог:**
```
=== GearSolver — Интерактивный режим ===

Количество шестерён (1 или 2)? 1

Для каждого параметра введите значение или 0, если параметр неизвестен.

=== Шестерня 1 ===
  da1 (диаметр вершин, мм): 52
  df1 (диаметр впадин, мм): 46
  z1 (число зубьев): 20

aw (межосевое расстояние, мм, опционально): 0
```

**Введение нулей для неизвестных параметров:**

Если известны только некоторые параметры:

```
  da1 (диаметр вершин, мм): 50
  df1 (диаметр впадин, мм): 0        ← неизвестно, введено 0
  z1 (число зубьев): 20
```

Система найдёт модули, которые удовлетворяют имеющимся ограничениям.

---

## 🎯 Использованные формулы

### Основные соотношения

```
m  = модуль (шаг зацепления в мм)
z  = число зубьев
x  = коэффициент смещения инструмента (-1.5 ≤ x ≤ +1.5)
ha* = относительная высота головки зуба (0.5 ≤ ha* ≤ 1.2)
c*  = относительный радиальный зазор (0.15 ≤ c* ≤ 0.4)
```

### Диаметры

```
d_делительный = m * z
d_вершин (da) = m * (z + 2*ha* + 2*x)
d_впадин (df) = m * (z - 2*(ha* + c*) + 2*x)
```

### Для пары шестерён

```
Межосевое расстояние: aw = m*(z1 + z2)/2 + m*(x1 + x2)
```

---

## 📊 Анализ дашборда

После выполнения скрипта открывается `gear_results.html` с двумя интерактивными графиками.

### График 1: Модуль vs Ошибка

**Что видите:**
- Линия показывает кривую ошибки при изменении модуля
- Маркеры обозначают отдельные модули:
  - ⭐ звёздочка = ГОСТ стандарт
  - 🔺 треугольник = дюймовый питч (DP)
  - 🔵 круг = произвольный модуль

**Интерпретация:**
- **Впадины** на линии — идеальные модули с минимальной ошибкой
- Выбирайте модули со звёздочками (ГОСТ) если возможно
- Острые пики ошибки — модули, неподходящие для данного параметра

**Пример:** На графике видна впадина при m=2.25 со звёздочкой → это ГОСТ модуль 2.25, идеально подходящий.

### График 2: Смещение vs Модуль (цветная карта)

**Что видите:**
- X-ось: модуль `m` (мм)
- Y-ось: найденное оптимальное смещение `x`
- Цвет: величина ошибки (жёлтый = маленькая, фиолетовый = большая)

**Интерпретация:**
- **Жёлтые кластеры** (низкая ошибка) — идеальные зоны
- **Особенность:** ГОСТ модули часто требуют больших смещений
- **Парето фронт:** верхняя граница жёлтых точек = лучшие компромиссы
- **Паттерны:** 
  - Линейный рост смещения с модулем → нормальное поведение
  - Скачки цвета → переход между локальными минимумами

**Практическое применение:**
Если у вас есть ограничение на величину смещения (скажем, |x| < 0.5):
1. Найдите на графике полосу y ∈ [-0.5, +0.5]
2. В этой полосе ищите жёлтые точки (минимальная ошибка)
3. Выбирайте модуль с самой жёлтой точкой

---

## ⚙️ Настройка под конкретные требования

### Пример: Узкий диапазон модулей

Если нужны модули от 3 до 5 мм, отредактируйте `config.py`:

```python
MODULE_MIN = 3.0  # было 0.5
MODULE_MAX = 5.0  # было 10.0
```

### Пример: Более жесткий допуск

Если критична высокая точность (до 0.1 мм):

```python
TOLERANCE = 0.1  # было 0.2 или 5.0
```

**Внимание:** Меньший допуск → меньше результатов в таблице!

### Пример: Ограничение на смещение

Если производство не позволяет смещения более ±1.0:

```python
X_BOUNDS = (-1.0, 1.0)  # было (-1.5, 1.5)
```

Система будет искать решения только в этом диапазоне.

### Пример: Стандартные инструменты

Если используются только ГОСТ инструменты, удалите DP питчи:

```python
DP_PITCHES = []  # пусто, не подключаем DP
```

---

## 🔧 Типичные задачи

### Задача 1: Найти модуль для известной шестерни

**Дано:** Шестерня z=24 с диаметром вершин da=50мм

```bash
python gear_calc.py 50 0 24
```

**Расчёт:** m = da / (z + 2*ha* + 2*x) ≈ 2.0 мм при стандартных ha*=1.0, x=0

**Результат:** Скрипт найдёт m≈2.0 с минимальной ошибкой.

### Задача 2: Спроектировать пару с нужным передаточным числом

**Дано:** 
- Малая шестерня: z₁=20
- Большая шестерня: z₂=60 (передаточное число i=3)
- Нужное межосевое расстояние: aw=100мм

```bash
python gear_calc.py 0 0 20 0 0 60 100
```

Введите 0 для неизвестных диаметров. Система найдёт модуль и смещения, обеспечивающие межосевое расстояние aw=100мм.

### Задача 3: Выбрать стандартный инструмент

После анализа одной шестерни в таблице:

1. Ищите результаты со звёздочкой ГОСТ (они стоят дешевле)
2. Если звёздочек нет — ищите DP маркировку (дюймовые инструменты)
3. Выбирайте модуль с минимальной ошибкой в строке с маркировкой

**Пример:** Лучший результат m=2.400 (обычный), но m=2.250 (ГОСТ ⭐) имеет ошибку 0.0000 — выбирайте ГОСТ!

---

## 📈 Интерпретация таблицы результатов

```
│ 1 │    2.400 │     —      │  0.285 │      — │   0.548 │  0.154 │      0.0000 │
│ 2 │    2.250 │   ГОСТ *   │  0.982 │      — │   0.573 │  0.187 │      0.0000 │
```

**Сравнение результатов 1 и 2:**

| Аспект | Result 1 | Result 2 | Вывод |
|--------|----------|----------|-------|
| Модуль | 2.400 (нестандартный) | 2.250 (ГОСТ) | Result 2 лучше (стандартный) |
| Ошибка | 0.0000 | 0.0000 | Одинаковая точность |
| Смещение | x=+0.285 (малое) | x=+0.982 (большое) | Result 1 требует меньше смещения |
| Геометрия | ha*=0.548 | ha*=0.573 | Result 1 дешевле в обработке |

**Практический выбор:** 
- Если нет критичного ограничения на смещение → выберите Result 2 (ГОСТ дешевле)
- Если смещение критично (сложная механика) → выберите Result 1
- Компромисс: посмотрите Result 3, 4, ... может быть лучше по всем параметрам

---

## 🎓 Образовательная ценность

Код GearSolver демонстрирует:

1. **Многопроцессорность:** `ProcessPoolExecutor` для параллельных вычислений
2. **Оптимизация:** `scipy.optimize.least_squares` с несколькими стартовыми точками
3. **Обработка данных:** NumPy для векторных операций
4. **Визуализация:** Plotly для интерактивных графиков
5. **CLI парсинг:** `argparse` и интерактивный ввод
6. **Type hints:** Полная типизация Python 3.13+
7. **Архитектура:** Разделение на модули с чистыми интерфейсами

Идеален для обучения комплексному проектированию Python-приложений.

---

**Больше информации в [`README.md`](README.md)**
